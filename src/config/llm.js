module.exports = {
  gemini: {
    apiKey: process.env.GEMINI_API_KEY,
    // For text-only input, use the gemini-pro model
    // modelName: 'gemini-pro',
    modelName: 'gemini-2.0-flash',
  },
  // Add configuration for other models here
}; 